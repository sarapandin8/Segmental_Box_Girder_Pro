# Segmental Box Girder Pro — COMMERCIAL.LOADS.16

This baseline carries forward the previously accepted commercial milestones and standards.

Current milestone focus:
- Clarify Loads numbering by labeling the combined rail-action page as `3.4 LF / 3.5 HF` so section 3.5 is not visually skipped.
- Replace the cropped raster wind factor/deck-height reference with an app-rendered vector reference card.
- Keep the lower deck-height `ze` schematic fully visible inside the Wind Input Assistant card.
- Preserve all existing wind, LF/HF, and load calculation logic.

Clean package rule:
- ZIP releases must not include `__pycache__/`, `.pytest_cache/`, `*.pyc`, `*.pyo`, `.streamlit/`, or `.DS_Store`.
